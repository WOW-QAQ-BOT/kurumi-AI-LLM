# -*- coding: utf-8 -*-
"""时崎狂三 · 记忆与存储(memory 系列)

本包放"记忆这个东西本身"的模型与持久化,不含角色业务规则(那在 ``kurumi.memory``):

- ``model``     记忆的数据模型与纯函数(**不含 IO**)
- ``store``     记忆与会话的 SQLite 存储(含 schema 迁移)
- ``commands``  ``/记忆`` ``/忘记`` ``/纠正`` ``/置顶`` 命令的解析与执行

依赖方向是单向的:本包不导入界面,也只在函数内按需导入 ``kurumi.memory``。
"""
