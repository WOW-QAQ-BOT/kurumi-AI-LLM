# -*- coding: utf-8 -*-
"""时崎狂三 · 角色运行时(kurumi 系列)

本包放"这一位角色"的业务逻辑(与界面、与底层存储都无关):

- ``context``      上下文预算与结构化组装(人设 + 记忆 + 知识库 + 转录)
- ``conversation`` 会话服务:轮次、回滚、裁剪、计数的唯一所有者
- ``memory``       长期记忆的提炼 / 去重 / 防误清空 / JSON 镜像

界面(ui 包)与存储层(memory 包)都不在这里。
"""
